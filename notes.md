fast api standard extras - uvcorn, fastapi framework, fastapi cli command used to run our app.


first video:
sending json responses
html responses
multiple routes returning the same data.
second video:

